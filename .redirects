# This file is copied into the public folder during the build step and removed during dev.
# Add redirects in netlify.toml instead — the Remix server function routing is handled
# automatically by the Netlify Vite adapter's v2 function config (path: "/*").